namespace IDI.Core.Extensions
{
    public static class EnumExtension
    {
        //public static List<T> GetValues<T>(this Type type) where T : class
        //{
        //    if (!type.IsEnum || type != typeof(T))
        //        return new List<T>();

        //    return Enum.GetValues(typeof(T)).Cast<T>().ToList();
        //}
    }
}
